# GitHub personal access token (fine-grained, scoped to selene + openclaw repos)
GITHUB_TOKEN=

# SQLite database path (defaults to ./dashboard.db in project root)
# Use absolute path when running via pm2
# DB_PATH=/Users/you/projects/dashboard/dashboard.db
